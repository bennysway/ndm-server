{
  "name": "ndm-server",
  "version": "1.0.0",
  "description": "Server for NDM",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start" : "node server.js"
  },
  "author": "bennysway",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.1"
  }
}
